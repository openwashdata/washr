% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setup_ci.R
\name{setup_ci}
\alias{setup_ci}
\title{Set up the R CMD check workflow for the data package}
\usage{
setup_ci()
}
\value{
The path of the workflow file, invisibly.
}
\description{
\code{setup_ci()} writes the GitHub Actions workflow that runs \verb{R CMD check} on
every push and pull request to \code{main} and \code{dev}, on macOS, Windows and
three versions of R on Linux. The openwashdata review standard requires
this workflow with the \code{dev} trigger, so a package scaffolded with washr
passes that part of the review floor by construction.

The workflow lands in \code{.github/workflows/R-CMD-check.yaml} and \code{.github}
is added to \code{.Rbuildignore}. When \code{README.Rmd} exists with badge markers,
the matching R CMD check badge is added between them (the README template
from \code{\link[=setup_readme]{setup_readme()}} carries it already). An existing workflow file is
kept as it is, so the function is safe to run again.
}
\examples{
\dontrun{
setup_ci()
}
}
\seealso{
Next: \code{\link[=setup_rawdata]{setup_rawdata()}}. The badge appears in the README written by \code{\link[=setup_readme]{setup_readme()}}.

Other setup functions:
\code{\link[=setup_dictionary]{setup_dictionary()}},
\code{\link[=setup_rawdata]{setup_rawdata()}},
\code{\link[=setup_roxygen]{setup_roxygen()}}
}
\concept{setup functions}
